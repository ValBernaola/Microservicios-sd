server:
  port: ${PORT:4{SERVER_PORT:0}}
eureka:
  cliente:
    fetch-registry: true
    register-with-eureka: true
    service-url:
     default-sone: http://localhost:8761/aureka
     instance: 
       instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}