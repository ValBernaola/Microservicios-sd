server:
  port: 8083
eureka:
  cliente:
    fetch-registry: true
    register-with-eureka: true
    service-url:
     default-sone: http://localhost:8761/aureka
     instance: 
       hostname: localhost